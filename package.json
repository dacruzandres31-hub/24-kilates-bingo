{
  "name": "bingo-24k-platform",
  "version": "1.0.0",
  "description": "Bingo 24K - Plataforma de juego en línea con sistema gamificado multinivel",
  "private": true,
  "workspaces": [
    "server",
    "client-player",
    "client-admin"
  ],
  "scripts": {
    "dev": "npm run dev -w client-player",
    "build": "npm run build -w server && npm run build -w client-player && npm run build -w client-admin",
    "start": "npm start -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "react-router-dom": "^7.10.1"
  }
}
